<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="MoleWindows.Pages.InstallerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:models="using:MoleWindows.Models"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{StaticResource MoleWindowsBackgroundBrownBrush}"
    Loaded="InstallerPage_Loaded"
    mc:Ignorable="d">

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="112" />
            <RowDefinition Height="*" />
            <RowDefinition Height="78" />
        </Grid.RowDefinitions>

        <Grid Padding="24,0" ColumnSpacing="18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Spacing="18">
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Installers" />
                <TextBlock
                    VerticalAlignment="Center"
                    FontFamily="{StaticResource MoleWindowsMonoFont}"
                    FontSize="18"
                    FontWeight="Bold"
                    Foreground="{StaticResource MoleWindowsDimBrush}"
                    Text="{Binding Summary}" />
            </StackPanel>

            <StackPanel
                Grid.Column="2"
                VerticalAlignment="Center"
                Orientation="Horizontal"
                Spacing="12">
                <Button
                    Command="{Binding ScanCommand}"
                    Content="Scan"
                    Style="{StaticResource MoleWindowsPillButtonStyle}" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" BorderBrush="{StaticResource MoleWindowsStrokeBrush}" BorderThickness="0,1,0,1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="118" />
            </Grid.RowDefinitions>

            <ListView
                Padding="24,0"
                ItemsSource="{Binding Items}"
                SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:InstallerCleanupCandidate">
                        <Grid MinHeight="94" ColumnSpacing="18">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="48" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="132" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="52" />
                            </Grid.ColumnDefinitions>

                            <FontIcon
                                VerticalAlignment="Center"
                                FontSize="28"
                                Foreground="{StaticResource MoleWindowsGoldBrush}"
                                Glyph="&#xE8B7;" />

                            <StackPanel
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Spacing="4">
                                <TextBlock
                                    FontSize="22"
                                    FontWeight="SemiBold"
                                    Foreground="{StaticResource MoleWindowsTextBrush}"
                                    Text="{x:Bind Name}"
                                    TextTrimming="CharacterEllipsis" />
                                <TextBlock
                                    FontFamily="{StaticResource MoleWindowsMonoFont}"
                                    FontSize="14"
                                    Foreground="{StaticResource MoleWindowsDimBrush}"
                                    Text="{x:Bind DetailLine}"
                                    TextTrimming="CharacterEllipsis" />
                            </StackPanel>

                            <TextBlock
                                Grid.Column="2"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                FontFamily="{StaticResource MoleWindowsMonoFont}"
                                FontSize="18"
                                FontWeight="Bold"
                                Foreground="{StaticResource MoleWindowsGoldBrush}"
                                Text="{x:Bind SizeText}" />

                            <TextBlock
                                Grid.Column="3"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                FontFamily="{StaticResource MoleWindowsMonoFont}"
                                FontSize="14"
                                Foreground="{StaticResource MoleWindowsMutedBrush}"
                                Text="{x:Bind LastTouchedText}" />

                            <CheckBox
                                Grid.Column="4"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                IsChecked="{x:Bind IsSelected, Mode=TwoWay}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Grid
                Grid.Row="1"
                Padding="24,12"
                BorderBrush="{StaticResource MoleWindowsStrokeBrush}"
                BorderThickness="0,1,0,0"
                ColumnSpacing="18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center" Spacing="6">
                    <TextBlock
                        FontFamily="{StaticResource MoleWindowsMonoFont}"
                        FontSize="16"
                        FontWeight="Bold"
                        Foreground="{StaticResource MoleWindowsMutedBrush}"
                        Text="{Binding SelectedSummary}" />
                    <TextBlock
                        FontFamily="{StaticResource MoleWindowsMonoFont}"
                        FontSize="12"
                        Foreground="{StaticResource MoleWindowsDimBrush}"
                        Text="{Binding EngineSummary}"
                        TextTrimming="CharacterEllipsis" />
                </StackPanel>

                <TextBox
                    Grid.Column="1"
                    Width="420"
                    Height="84"
                    AcceptsReturn="True"
                    FontFamily="{StaticResource MoleWindowsMonoFont}"
                    FontSize="12"
                    IsReadOnly="True"
                    Text="{Binding OutputText, Mode=OneWay}"
                    TextWrapping="Wrap" />
            </Grid>
        </Grid>

        <Grid Grid.Row="2" Padding="24,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Spacing="8">
                <ProgressRing
                    Width="22"
                    Height="22"
                    IsActive="{Binding IsBusy}" />
                <TextBlock
                    VerticalAlignment="Center"
                    FontFamily="{StaticResource MoleWindowsMonoFont}"
                    FontSize="18"
                    FontWeight="Bold"
                    Foreground="{StaticResource MoleWindowsMutedBrush}"
                    Text="old Downloads installers" />
            </StackPanel>

            <Button
                Grid.Column="1"
                Margin="0,0,12,0"
                Command="{Binding ClearSelectionCommand}"
                Content="clear"
                Style="{StaticResource MoleWindowsSecondaryPillButtonStyle}" />
            <Button
                Grid.Column="2"
                Margin="0,0,12,0"
                Command="{Binding SelectAllCommand}"
                Content="select all"
                Style="{StaticResource MoleWindowsSecondaryPillButtonStyle}" />
            <Button
                Grid.Column="3"
                Margin="0,0,12,0"
                Command="{Binding ScanCommand}"
                Content="Preview"
                Style="{StaticResource MoleWindowsSecondaryPillButtonStyle}" />
            <Button
                Grid.Column="4"
                Click="RemoveButton_Click"
                Content="Remove"
                IsEnabled="{Binding CanRemove}"
                Style="{StaticResource MoleWindowsPillButtonStyle}" />
        </Grid>
    </Grid>
</Page>
