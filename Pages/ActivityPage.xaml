<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinMoe.Pages.ActivityPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:models="using:WinMoe.Models"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{StaticResource WinMoeBackgroundGreenBrush}"
    Loaded="ActivityPage_Loaded"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="活动" />
            <TextBlock
                Grid.Column="1"
                VerticalAlignment="Center"
                FontFamily="{StaticResource WinMoeMonoFont}"
                FontSize="18"
                FontWeight="Bold"
                Foreground="{StaticResource WinMoeDimBrush}"
                Text="{Binding Summary}" />
            <Button
                Grid.Column="2"
                Command="{Binding RefreshCommand}"
                Style="{StaticResource WinMoeIconButtonStyle}">
                <FontIcon Glyph="&#xE72C;" />
            </Button>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource WinMoeCardBorderStyle}">
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock
                    FontFamily="{StaticResource WinMoeMonoFont}"
                    Foreground="{StaticResource WinMoeDimBrush}"
                    Text="{Binding HistoryPath}"
                    TextWrapping="Wrap" />
                <ListView Grid.Row="1" ItemsSource="{Binding Entries}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:OperationHistoryEntry">
                            <Grid MinHeight="70" ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="42" />
                                    <ColumnDefinition Width="170" />
                                    <ColumnDefinition Width="150" />
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Ellipse
                                    Width="12"
                                    Height="12"
                                    Fill="{StaticResource WinMoeGreenBrush}" />
                                <TextBlock
                                    Grid.Column="1"
                                    FontFamily="{StaticResource WinMoeMonoFont}"
                                    Foreground="{StaticResource WinMoeMutedBrush}"
                                    Text="{x:Bind TimestampText}" />
                                <TextBlock
                                    Grid.Column="2"
                                    FontWeight="SemiBold"
                                    Text="{x:Bind Operation}"
                                    TextTrimming="CharacterEllipsis" />
                                <TextBlock
                                    Grid.Column="3"
                                    Foreground="{StaticResource WinMoeGoldBrush}"
                                    Text="{x:Bind ResultText}" />
                                <TextBlock
                                    Grid.Column="4"
                                    FontFamily="{StaticResource WinMoeMonoFont}"
                                    Foreground="{StaticResource WinMoeDimBrush}"
                                    Text="{x:Bind Summary}"
                                    TextTrimming="CharacterEllipsis" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
