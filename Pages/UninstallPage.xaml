<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="MoleWindows.Pages.UninstallPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:models="using:MoleWindows.Models"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:MoleWindows.ViewModels"
    x:Name="AppsRoot"
    Background="{StaticResource MoleWindowsBackgroundSoftwareBrush}"
    mc:Ignorable="d"
    Loaded="UninstallPage_Loaded">

    <Page.Resources>
        <Style x:Key="AppsListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
        </Style>

        <Style x:Key="AppsSearchBoxStyle" TargetType="TextBox">
            <Setter Property="Width" Value="320" />
            <Setter Property="Height" Value="42" />
            <Setter Property="Padding" Value="18,8" />
            <Setter Property="CornerRadius" Value="22" />
            <Setter Property="Background" Value="#14100E" />
            <Setter Property="Foreground" Value="{StaticResource MoleWindowsTextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource MoleWindowsStrokeBrush}" />
            <Setter Property="FontFamily" Value="{StaticResource MoleWindowsMonoFont}" />
            <Setter Property="FontSize" Value="15" />
        </Style>

        <Style x:Key="AppsSmallPillStyle" TargetType="Border">
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="CornerRadius" Value="18" />
            <Setter Property="Background" Value="#463124" />
        </Style>
    </Page.Resources>

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="92" />
            <RowDefinition Height="*" />
            <RowDefinition Height="72" />
        </Grid.RowDefinitions>

        <Grid Padding="24,0" ColumnSpacing="18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="72" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border Width="56" Height="56" VerticalAlignment="Center" CornerRadius="16">
                <Image Source="ms-appx:///Assets/Hero/software.png" Stretch="UniformToFill" />
            </Border>

            <Border
                Grid.Column="1"
                VerticalAlignment="Center"
                Padding="4"
                Background="#1A120F"
                BorderBrush="{StaticResource MoleWindowsStrokeBrush}"
                BorderThickness="1"
                CornerRadius="30">
                <StackPanel Orientation="Horizontal" Spacing="4">
                    <Button
                        x:Name="UninstallTabButton"
                        Click="AppsTabButton_Click"
                        Content="卸载"
                        Style="{StaticResource MoleWindowsTopNavButtonSelectedStyle}"
                        Tag="uninstall" />
                    <Button
                        x:Name="UpdatesTabButton"
                        Click="AppsTabButton_Click"
                        Content="更新"
                        Style="{StaticResource MoleWindowsTopNavButtonStyle}"
                        Tag="updates" />
                    <Button
                        x:Name="StartupTabButton"
                        Click="AppsTabButton_Click"
                        Content="启动项"
                        Style="{StaticResource MoleWindowsTopNavButtonStyle}"
                        Tag="startup" />
                </StackPanel>
            </Border>

            <Grid
                Grid.Column="2"
                Width="456"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                ColumnSpacing="14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button
                    x:Name="NameSortButton"
                    HorizontalAlignment="Stretch"
                    MinWidth="112"
                    Command="{Binding SortCommand}"
                    CommandParameter="name"
                    Content="名称"
                    Style="{StaticResource MoleWindowsTopNavButtonStyle}" />
                <Button
                    x:Name="SizeSortButton"
                    Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    MinWidth="112"
                    Command="{Binding SortCommand}"
                    CommandParameter="size"
                    Content="大小"
                    Style="{StaticResource MoleWindowsTopNavButtonSelectedStyle}" />
                <Button
                    x:Name="SourceSortButton"
                    Grid.Column="2"
                    HorizontalAlignment="Stretch"
                    MinWidth="112"
                    Command="{Binding SortCommand}"
                    CommandParameter="source"
                    Content="来源"
                    Style="{StaticResource MoleWindowsTopNavButtonStyle}" />
                <Button
                    Grid.Column="3"
                    Command="{Binding LoadCommand}"
                    Content="&#xE72C;"
                    FontFamily="Segoe MDL2 Assets"
                    Style="{StaticResource MoleWindowsIconButtonStyle}" />
            </Grid>

            <TextBox
                Grid.Column="3"
                VerticalAlignment="Center"
                PlaceholderText="搜索软件"
                Style="{StaticResource AppsSearchBoxStyle}"
                Text="{Binding SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <Border Grid.Row="1" BorderBrush="{StaticResource MoleWindowsStrokeBrush}" BorderThickness="0,1,0,1">
            <Grid>
                <Grid x:Name="UninstallContent">
                    <ListView
                        Padding="24,16,24,0"
                        ItemContainerStyle="{StaticResource AppsListViewItemStyle}"
                        ItemsSource="{Binding Applications}"
                        SelectedItem="{Binding SelectedApplicationRow, Mode=TwoWay}"
                        SelectionMode="Single">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="viewModels:ApplicationRowViewModel">
                                <Grid RowSpacing="0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <Grid MinHeight="88" Padding="0,8,18,8" ColumnSpacing="18">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="44" />
                                            <ColumnDefinition Width="60" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="220" />
                                            <ColumnDefinition Width="52" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            FontFamily="{StaticResource MoleWindowsMonoFont}"
                                            FontSize="26"
                                            FontWeight="Bold"
                                            Foreground="{StaticResource MoleWindowsDimBrush}"
                                            Text="{x:Bind ChevronText, Mode=OneWay}" />

                                        <Border
                                            Grid.Column="1"
                                            Width="44"
                                            Height="44"
                                            VerticalAlignment="Center"
                                            Background="#263646"
                                            CornerRadius="9">
                                            <TextBlock
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                FontSize="18"
                                                FontWeight="Bold"
                                                Foreground="{StaticResource MoleWindowsTextBrush}"
                                                Text="{x:Bind Initials}" />
                                        </Border>

                                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                            <TextBlock
                                                FontSize="22"
                                                FontWeight="SemiBold"
                                                Foreground="{StaticResource MoleWindowsTextBrush}"
                                                Text="{x:Bind Name}"
                                                TextTrimming="CharacterEllipsis" />
                                            <TextBlock
                                                FontFamily="{StaticResource MoleWindowsMonoFont}"
                                                FontSize="16"
                                                FontWeight="Bold"
                                                Foreground="{StaticResource MoleWindowsDimBrush}"
                                                Text="{x:Bind DetailLine}"
                                                TextTrimming="CharacterEllipsis" />
                                        </StackPanel>

                                        <TextBlock
                                            Grid.Column="3"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Center"
                                            FontFamily="{StaticResource MoleWindowsMonoFont}"
                                            FontSize="18"
                                            FontWeight="Bold"
                                            Foreground="{StaticResource MoleWindowsMutedBrush}"
                                            Text="{x:Bind RightSummary}" />

                                        <Ellipse
                                            Grid.Column="4"
                                            Width="31"
                                            Height="31"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Stroke="{StaticResource MoleWindowsDimBrush}"
                                            StrokeThickness="2.5" />
                                    </Grid>

                                    <Border
                                        Grid.Row="1"
                                        Margin="54,0,20,20"
                                        Padding="22"
                                        Background="#151110"
                                        BorderBrush="{StaticResource MoleWindowsStrokeBrush}"
                                        BorderThickness="1"
                                        CornerRadius="18"
                                        Visibility="{x:Bind DetailVisibility, Mode=OneWay}">
                                        <Grid RowSpacing="14">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="*" />
                                            </Grid.RowDefinitions>

                                            <Grid ColumnSpacing="14">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock
                                                        FontSize="22"
                                                        FontWeight="SemiBold"
                                                        Foreground="{StaticResource MoleWindowsTextBrush}"
                                                        Text="{x:Bind Name}"
                                                        TextTrimming="CharacterEllipsis" />
                                                    <TextBlock
                                                        FontFamily="{StaticResource MoleWindowsMonoFont}"
                                                        FontSize="16"
                                                        FontWeight="Bold"
                                                        Foreground="{StaticResource MoleWindowsDimBrush}"
                                                        Text="{x:Bind InstallLocation}"
                                                        TextTrimming="CharacterEllipsis" />
                                                </StackPanel>
                                                <TextBlock
                                                    Grid.Column="1"
                                                    VerticalAlignment="Center"
                                                    FontFamily="{StaticResource MoleWindowsMonoFont}"
                                                    FontSize="17"
                                                    FontWeight="Bold"
                                                    Foreground="{StaticResource MoleWindowsMutedBrush}"
                                                    Text="{Binding DataContext.LeftoverSelectionText, ElementName=AppsRoot}" />
                                                <Button
                                                    Grid.Column="2"
                                                    Command="{Binding DataContext.SelectAllLeftoversCommand, ElementName=AppsRoot}"
                                                    Content="全选"
                                                    Foreground="{StaticResource MoleWindowsOrangeBrush}"
                                                    Style="{StaticResource MoleWindowsTopNavButtonStyle}" />
                                            </Grid>

                                            <StackPanel
                                                Grid.Row="1"
                                                Orientation="Horizontal"
                                                Spacing="12">
                                                <Button
                                                    Command="{Binding DataContext.PreviewLeftoversCommand, ElementName=AppsRoot}"
                                                    Content="预览残留"
                                                    Style="{StaticResource MoleWindowsSecondaryPillButtonStyle}" />
                                                <TextBlock
                                                    VerticalAlignment="Center"
                                                    FontFamily="{StaticResource MoleWindowsMonoFont}"
                                                    FontWeight="Bold"
                                                    Foreground="{StaticResource MoleWindowsDimBrush}"
                                                    Text="{Binding DataContext.LeftoverSummary, ElementName=AppsRoot}" />
                                            </StackPanel>

                                            <TextBlock
                                                Grid.Row="2"
                                                FontFamily="{StaticResource MoleWindowsMonoFont}"
                                                FontSize="16"
                                                FontWeight="Bold"
                                                Foreground="{StaticResource MoleWindowsDimBrush}"
                                                Text="默认不选择，确认后移入回收站" />

                                            <ListView
                                                Grid.Row="3"
                                                MaxHeight="260"
                                                ItemContainerStyle="{StaticResource AppsListViewItemStyle}"
                                                ItemsSource="{Binding DataContext.Leftovers, ElementName=AppsRoot}"
                                                SelectionMode="None">
                                                <ListView.ItemTemplate>
                                                    <DataTemplate x:DataType="models:LeftoverCandidate">
                                                        <Grid MinHeight="42" ColumnSpacing="14">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="42" />
                                                                <ColumnDefinition Width="180" />
                                                                <ColumnDefinition Width="100" />
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="36" />
                                                            </Grid.ColumnDefinitions>
                                                            <CheckBox IsChecked="{x:Bind IsSelected, Mode=TwoWay}" />
                                                            <TextBlock
                                                                Grid.Column="1"
                                                                VerticalAlignment="Center"
                                                                FontSize="17"
                                                                FontWeight="SemiBold"
                                                                Foreground="{StaticResource MoleWindowsMutedBrush}"
                                                                Text="{x:Bind Category}"
                                                                TextTrimming="CharacterEllipsis" />
                                                            <TextBlock
                                                                Grid.Column="2"
                                                                VerticalAlignment="Center"
                                                                FontFamily="{StaticResource MoleWindowsMonoFont}"
                                                                FontWeight="Bold"
                                                                Foreground="{StaticResource MoleWindowsDimBrush}"
                                                                Text="{x:Bind SizeText}" />
                                                            <TextBlock
                                                                Grid.Column="3"
                                                                VerticalAlignment="Center"
                                                                FontFamily="{StaticResource MoleWindowsMonoFont}"
                                                                FontWeight="Bold"
                                                                Foreground="{StaticResource MoleWindowsDimBrush}"
                                                                Text="{x:Bind Path}"
                                                                TextTrimming="CharacterEllipsis" />
                                                            <FontIcon
                                                                Grid.Column="4"
                                                                HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"
                                                                FontSize="16"
                                                                Foreground="{StaticResource MoleWindowsDimBrush}"
                                                                Glyph="&#xE721;" />
                                                        </Grid>
                                                    </DataTemplate>
                                                </ListView.ItemTemplate>
                                            </ListView>
                                        </Grid>
                                    </Border>

                                    <Border
                                        Height="1"
                                        Margin="98,0,18,0"
                                        VerticalAlignment="Bottom"
                                        Background="#332622" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>

                <Grid x:Name="UpdatesContent" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="118" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Padding="24,0" ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource MoleWindowsMonoFont}"
                            FontSize="22"
                            FontWeight="Bold"
                            Foreground="{StaticResource MoleWindowsMutedBrush}"
                            Text="0 个可用更新" />
                        <StackPanel
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Orientation="Horizontal"
                            Spacing="10">
                            <ProgressRing
                                Width="26"
                                Height="26"
                                IsActive="{Binding IsBusy}" />
                            <TextBlock
                                VerticalAlignment="Center"
                                FontFamily="{StaticResource MoleWindowsMonoFont}"
                                FontSize="16"
                                FontWeight="Bold"
                                Foreground="{StaticResource MoleWindowsDimBrush}"
                                Text="正在检查…" />
                        </StackPanel>
                        <Button
                            Grid.Column="2"
                            Click="UnsupportedAppsFeatureButton_Click"
                            Content="检查更新"
                            Style="{StaticResource MoleWindowsPillButtonStyle}" />
                    </Grid>

                    <ScrollViewer Grid.Row="1">
                        <StackPanel Padding="36,28,36,36" Spacing="26">
                            <TextBlock
                                FontFamily="{StaticResource MoleWindowsMonoFont}"
                                FontSize="18"
                                FontWeight="Bold"
                                Foreground="{StaticResource MoleWindowsDimBrush}"
                                Text="支持安全更新源的软件 0" />
                            <Border
                                Padding="24"
                                Background="#14100E"
                                BorderBrush="{StaticResource MoleWindowsStrokeBrush}"
                                BorderThickness="1"
                                CornerRadius="18">
                                <Grid ColumnSpacing="18">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="54" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <FontIcon
                                        VerticalAlignment="Center"
                                        FontSize="28"
                                        Foreground="{StaticResource MoleWindowsOrangeBrush}"
                                        Glyph="&#xE895;" />
                                    <StackPanel Grid.Column="1">
                                        <TextBlock
                                            FontSize="22"
                                            FontWeight="SemiBold"
                                            Text="Mole Windows 软件更新" />
                                        <TextBlock
                                            FontFamily="{StaticResource MoleWindowsMonoFont}"
                                            FontSize="16"
                                            FontWeight="Bold"
                                            Foreground="{StaticResource MoleWindowsDimBrush}"
                                            Text="{Binding UpdatesSummary}"
                                            TextWrapping="Wrap" />
                                    </StackPanel>
                                    <Border Grid.Column="2" Style="{StaticResource AppsSmallPillStyle}">
                                        <TextBlock
                                            FontFamily="{StaticResource MoleWindowsMonoFont}"
                                            FontWeight="Bold"
                                            Foreground="{StaticResource MoleWindowsOrangeBrush}"
                                            Text="预览中" />
                                    </Border>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>

                <Grid x:Name="StartupContent" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="118" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Padding="24,0">
                        <TextBlock
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource MoleWindowsMonoFont}"
                            FontSize="22"
                            FontWeight="Bold"
                            Foreground="{StaticResource MoleWindowsMutedBrush}"
                            Text="启动项清单" />
                    </Grid>

                    <ScrollViewer Grid.Row="1">
                        <StackPanel Padding="36,28,36,36" Spacing="26">
                            <TextBlock
                                FontFamily="{StaticResource MoleWindowsMonoFont}"
                                FontSize="18"
                                FontWeight="Bold"
                                Foreground="{StaticResource MoleWindowsDimBrush}"
                                Text="WINDOWS 启动项 0" />
                            <Border
                                Padding="24"
                                Background="#14100E"
                                BorderBrush="{StaticResource MoleWindowsStrokeBrush}"
                                BorderThickness="1"
                                CornerRadius="18">
                                <Grid ColumnSpacing="18">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="54" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <FontIcon
                                        VerticalAlignment="Center"
                                        FontSize="28"
                                        Foreground="{StaticResource MoleWindowsGoldBrush}"
                                        Glyph="&#xE7C1;" />
                                    <StackPanel Grid.Column="1">
                                        <TextBlock
                                            FontSize="22"
                                            FontWeight="SemiBold"
                                            Text="启动项管理" />
                                        <TextBlock
                                            FontFamily="{StaticResource MoleWindowsMonoFont}"
                                            FontSize="16"
                                            FontWeight="Bold"
                                            Foreground="{StaticResource MoleWindowsDimBrush}"
                                            Text="{Binding StartupSummary}"
                                            TextWrapping="Wrap" />
                                    </StackPanel>
                                    <Border Grid.Column="2" Style="{StaticResource AppsSmallPillStyle}">
                                        <TextBlock
                                            FontFamily="{StaticResource MoleWindowsMonoFont}"
                                            FontWeight="Bold"
                                            Foreground="{StaticResource MoleWindowsGoldBrush}"
                                            Text="待接入" />
                                    </Border>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Padding="24,0" Background="#100E0D">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                VerticalAlignment="Center"
                FontFamily="{StaticResource MoleWindowsMonoFont}"
                FontSize="20"
                FontWeight="Bold"
                Foreground="{StaticResource MoleWindowsMutedBrush}"
                Text="{Binding AppsCountText}" />
            <Button
                Grid.Column="1"
                Margin="0,0,12,0"
                Click="RemoveLeftoversButton_Click"
                Content="移入回收站"
                IsEnabled="{Binding CanRemoveSelectedLeftovers}"
                Style="{StaticResource MoleWindowsSecondaryPillButtonStyle}" />
            <Button
                Grid.Column="2"
                Click="UninstallButton_Click"
                Content="启动卸载程序"
                IsEnabled="{Binding CanLaunchUninstaller}"
                Style="{StaticResource MoleWindowsPillButtonStyle}" />
        </Grid>
    </Grid>
</Page>
